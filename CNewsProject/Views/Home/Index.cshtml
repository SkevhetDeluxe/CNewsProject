<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"] - CNewsProject</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/CNewsProject.styles.css" asp-append-version="true" />
</head>
<body>   
    <main>
        <section class="main-article">
            <img src="main-article-image.jpg" alt="Main Article Image">
            <h1>HEADLINE - This might take a lot of space like this or something I guess?</h1>
            <p>Summary here - This article is DEFINITELY worth reading so click me click me click me!!!</p>
        </section>
        <aside>
            <div class="card">
                <h2>Weather</h2>
                <div class="weather-icon">☀️</div>
                <p>Weather details here...</p>
            </div>
            <div class="card">
                <h2>Exchange rates</h2>
                <ul>
                    <li>USD</li>
                    <li>EUR</li>
                    <li>JPY</li>
                    <li>CNY</li>
                    <li>GBP</li>
                </ul>
                <h2><a href="@Url.Action("Local", "News")">Local news</a></h2>
                <h2><a href="@Url.Action("Sweden", "News")">Swedish news</a></h2>
                <h2><a href="@Url.Action("Economy", "News")">Economy news</a></h2>                
            </div>
        </aside>
    </main>
</body>
</html>

<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script>
    $(document).ready(function () {
        var apiKey = 'YOUR_API_KEY'; // Replace with your OpenWeatherMap API key
        var city = 'Atlanta'; // Replace with the desired city
        var apiUrl = `https://api.openweathermap.org/data/2.5/weather?q=${city}&appid=${apiKey}&units=metric`;

        $.ajax({
            url: apiUrl,
            method: 'GET',
            success: function (data) {
                var weatherIcon = data.weather[0].icon;
                var weatherDescription = data.weather[0].description;
                var temperature = data.main.temp;
                var weatherDetails = `${weatherDescription}, ${temperature}°C`;

                $('#weather-details').text(weatherDetails);
            },
            error: function () {
                $('#weather-details').text('Unable to fetch weather details.');
            }
        });
    });
</script>