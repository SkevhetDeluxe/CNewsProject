@model IEnumerable<IdentityRole>
@{
    ViewData["Title"] = "ROLES";
}

<div class="vadSomHelst">

    <h3 class="">All Roles</h3>

    <a asp-action="Index" class="btn btn-secondary">Back to Base</a>
    <a asp-action="CreateRole" class="btn btn-secondary">Create a Role</a>


    <div class="">
        <table class="table table-sm table-bordered table-bordered mt-1" id="rolesTable" style="white-space: nowrap">
            <thead>
            <tr>
                <th>ID</th><th>Name</th><th>Users</th><th>Update</th><th>Delete</th>
            </tr>
            </thead>


            @* @foreach (var role in Model)
    {
    <tr>
    <td>@role.Id</td>
    <td>@role.Name</td>
    <td i-role="@role.Id"></td>

    <td>
    <a class="btn btn-sm btn-primary" asp-action="UpdateRole" asp-route-id="@role.Id">Update</a>
    </td>
    <td>
    <a class="btn btn-sm btn-danger" asp-action="DeleteRole" asp-route-id="@role.Id">Delete</a>
    </td>                


    </tr>

    } *@
            <tbody>


            @{
                List<string> vitalRoles = new() { "Admin", "The Publisher", "Journalist" };

                foreach (var role in Model)
                {
                    <tr>
                        <td>@role.Id</td>
                        <td>@role.Name</td>
                        <td i-role="@role.Name"></td>
                        <td>
                            <a class="btn btn-sm btn-primary" asp-action="UpdateRole" asp-route-id="@role.Id">Update</a>
                        </td>
                        <td>
                            @{
                                if (vitalRoles.Contains(role.Name))
                                {
                                    <a class="btn btn-sm btn-danger disabled" asp-action="DeleteRole" asp-route-id="@role.Name">Delete</a>
                                }
                                else
                                {
                                    <a class="btn btn-sm btn-danger" asp-action="DeleteRole" asp-route-id="@role.Id">Delete</a>
                                }
                            }

                        </td>

                    </tr>
                }
            }
            </tbody>
        </table>
    </div>
</div>