@model IEnumerable<IdentityRole>
@{
    ViewData["Title"] = "ROLES";
}

<h1 class="bg-info text-white">All Roles</h1>

<a asp-action="Index" class="btn btn-secondary">Back to Base</a>
<a asp-action="CreateRole" class="btn btn-secondary">Create a Role</a>

<table class="table table-sm table-bordered table-bordered">
    <tr><th>ID</th><th>Name</th><th>Users</th><th>Update</th><th>Delete</th></tr>

    @* @foreach (var role in Model)
    {
    <tr>
    <td>@role.Id</td>
    <td>@role.Name</td>
    <td i-role="@role.Id"></td>

    <td>
    <a class="btn btn-sm btn-primary" asp-action="UpdateRole" asp-route-id="@role.Id">Update</a>
    </td>
    <td>
    <a class="btn btn-sm btn-danger" asp-action="DeleteRole" asp-route-id="@role.Id">Delete</a>
    </td>

    </tr>
    } *@

    @{
        if (Model != null)
        {

            @foreach (var role in Model.Where(r => r.Name == "Admin" || r.Name == "Subscriber" || r.Name == "The Publisher" || r.Name == "Journalist"))
            {
                <tr>
                    <td>@role.Id</td>
                    <td>@role.Name</td>
                    <td i-role="@role.Id"></td>

                    <td>
                        <a class="btn btn-sm btn-primary" asp-action="UpdateRole" asp-route-id="@role.Id">Update</a>
                    </td>
                    <td>
                        <a class="btn btn-sm btn-danger disabled" asp-action="DeleteRole" asp-route-id="@role.Id">Delete</a>
                    </td>

                </tr>
            }


            @foreach (var role in Model.Where(r => r.Name != "Admin" && r.Name != "Subscriber" && r.Name != "The Publisher" && r.Name != "Journalist"))
            {
                <tr>
                    <td>@role.Id</td>
                    <td>@role.Name</td>
                    <td i-role="@role.Id"></td>

                    <td>
                        <a class="btn btn-sm btn-primary" asp-action="UpdateRole" asp-route-id="@role.Id">Update</a>
                    </td>
                    <td>
                        <a class="btn btn-sm btn-danger" asp-action="DeleteRole" asp-route-id="@role.Id">Delete</a>
                    </td>

                </tr>
            }
        }
    }

</table>